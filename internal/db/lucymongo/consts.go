package lucymongo

// EnvKeyURI is the env var name that the mongo URI to be used will be stored in.
const EnvKeyURI = "LUCY_MONGO_URI"

// EnvKeyDBName is the env var key the database name to use will be stored in.
const EnvKeyDBName = "LUCY_MONGO_DB_NAME"
